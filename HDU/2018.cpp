#include <iostream>
using namespace std;

int main(void) {
#ifdef LOCAL
    freopen("2018.in", "r", stdin);
#endif
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    while (cin >> n) {
        if (n == 0) break;

    }

    return 0;
}
