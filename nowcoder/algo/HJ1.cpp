#include <iostream>
#include <sstream>

using namespace std;

int main()
{
    // string s, t;
    // getline(cin, s);
    // stringstream ss(s);
    // while (ss >> t) ;
    // cout << t.size() << endl;

    string s;
    while (cin >> s);
    cout << s.size() << endl;
    return 0;
}
